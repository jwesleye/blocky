# Example environment variables. Copy to `.env` and adjust as needed.
# The app is client-only and currently requires no runtime env vars.
# Vite-exposed variables must be prefixed with VITE_.

# VITE_APP_TITLE=blocky
