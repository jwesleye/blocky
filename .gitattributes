# Normalize line endings to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Treat these as binary (never normalize / diff).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.glb binary
*.gltf binary
